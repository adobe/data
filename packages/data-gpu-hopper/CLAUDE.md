# CLAUDE.md — data-gpu-hopper

## Not a reference target (yet)

This package is a **work-in-progress sample** and has **not** been reviewed for
code quality. Do **not** treat it as a canonical example:

- Do not cite it as a reference/exemplar for the `@adobe/data` build skills or
  the `.claude/rules/` (AI rules). The reviewed samples are the reference
  targets, not this one.
- Do not copy its patterns into other packages or hold it up as "how it's done"
  until it has passed a quality review.

It exists to exercise the `@adobe/data-gpu` colored-cube rendering path end to
end; the game logic is conformance-tested, but the wiring and rendering layer
still need a review pass before this becomes reference-grade.
